#include "Array_List.h"
int main() {
	Array_List a;
	/*for (int b = 0; b <= 20; b++) {
		a.add(b);
	}
	a.remove(20);
	a.print_list();
	cout <<"                               "<< a.remove(10) << endl;*/
	cout<<a.get_value(25);
}