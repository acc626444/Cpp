#include "functions.h"
int main( ) {
	string in = "initial value";
	getString( in );
	cout << in << endl;
	skip( 3 );
	getstring( in );
	cout << in << endl;
	skip( 3 );
	getstring( in );
	cout << in << endl;
}